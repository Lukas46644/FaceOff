# FaceOff frontend

React 19 + Vite + MUI single-page app. See the [root README](../README.md) for setup, environment variables, and the full project overview.

```bash
npm install
npm run dev      # dev server on :5173
npm run build    # production build to dist/
npm run lint      # oxlint
```
